stages:
  - deprecated

deprecated_pipeline:
  stage: deprecated
  script:
    - echo "Este pipeline interno esta obsoleto."
    - echo "Usa el .gitlab-ci.yml de la raiz aula-virtual-docker para deploy a produccion."
  rules:
    - when: manual
      allow_failure: true
